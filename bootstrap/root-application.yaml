apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: fleet-dispatch-root
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: fleet-dispatch-gitops
  source:
    repoURL: https://github.com/linhdo04/gsm-fleet-dispatch-ai-gitops
    targetRevision: main
    path: applications
    kustomize:
      patches:
        - target:
            group: argoproj.io
            version: v1alpha1
            kind: Application
          patch: |-
            - op: replace
              path: /spec/source/repoURL
              value: https://github.com/linhdo04/gsm-fleet-dispatch-ai-gitops
            - op: replace
              path: /spec/source/targetRevision
              value: main
  destination:
    server: https://kubernetes.default.svc
    namespace: argocd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=false
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
