# Local secret values; only the example contract is committed.
apps/**/app-secrets.env

# Local render and downloaded bootstrap artifacts.
.rendered/
bootstrap/install.yaml

# Cluster credentials must never be committed.
*.kubeconfig
infrastructure/cert-manager/cloudflare-api-token-secret.yaml
infrastructure/monitoring/grafana-admin-secret.yaml

# Claude Code local workspace files.
.claude/
